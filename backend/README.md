# Smart Goal Planner Backend

JSON Server backend for the Smart Goal Planner application.

## Local Development
```bash
npm install
npm run dev
```

## Production
```bash
npm install
npm start
```

The server will run on the port specified by the PORT environment variable (provided by Render) or default to 3001 for local development.